# Dependencias de RUNTIME del servicio Athos (chat + phantom + health + auth).
# Sirve tanto para Railway (ahora) como para un futuro Vercel (bundle chico).
# NOTA: `llama-index` (en pyproject.toml) NO se usa en el código -> se omite aquí a propósito
# para mantener el bundle liviano. Si la ingesta llegara a necesitarla, agrégala.
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
psycopg[binary]
pgvector
httpx
# Cliente WebSocket para Deepgram Live (transcripcion EN VIVO del Modo Fantasma). Llegaba ya como
# dependencia transitiva de `uvicorn[standard]`, pero ahora el codigo lo importa directo
# (`app/streaming_transcription.py`) y una dependencia directa se declara.
websockets
pyjwt
anthropic
cohere
truststore>=0.10.4
cryptography>=49.0.0

psycopg_pool
